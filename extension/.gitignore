# Generated by build-chromium.mjs
dist/

# Written by the register-host scripts; they point at a machine-specific path,
# so they are per-install rather than per-repo.
com.nova.connector.json
com.nova.connector.chromium.json

# The CRX signing key. Only needed to publish an extension with the SAME id
# later; the public half lives in manifest.chromium.json and is not secret.
.chromium-key.pem
